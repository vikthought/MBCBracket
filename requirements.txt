# numpy pinned <2 because hdbscan's wheels target the numpy 1.x ABI.
# Drop the cap once hdbscan ships numpy-2-compatible wheels.
numpy>=1.24,<2
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0
hdbscan>=0.8.33
tabulate>=0.9
